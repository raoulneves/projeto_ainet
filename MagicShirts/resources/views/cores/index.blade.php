@extends('layout')
@section('title','Cores')
@section('content')
<h1> As Nossas Cores </h1>
     {{-- <div class="search-item">
            <label for="idCor">Cores:</label>
            <select name="cor" id="idCor">
                @foreach($cores as $cor)
                <div class="curso">
                    <div class="curso-info-area">
                        <div class="curso-info">
                            <span class="curso-label">id</span>
                            <span class="curso-info-desc">{{$cor->nome}}</span>
                        </div>
                    </div>
                </div>
                @endforeach
            </select>
        </div> --}}

@endsection
